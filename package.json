{
  "name": "custom-toasts",
  "version": "1.0.0",
  "description": "My custom toasts.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass -w scss/ -o css/ --recursive"
  },
  "author": "Yaroslav Yukhymchuk",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^5.0.0"
  }
}
